FamiPet STEP 7 — COMMUNITY IMAGE UPLOAD

🚨 FRONTEND FREEZE

Do NOT redesign Community.

Current problem:

«Users cannot attach images while posting.»

Trace:

Community Post
↓
Select Image
↓
FormData
↓
Backend Upload
↓
Image Storage
↓
Image URL
↓
Community Model
↓
MongoDB
↓
Community Page

Inspect the existing image-storage system.

If the project already uses Cloudinary/Multer/local upload, use it.

Check:

- File input
- FormData
- multipart/form-data
- Upload middleware
- File field name
- Image URL
- Community controller
- Community model
- MongoDB
- Frontend rendering

Do not expose API secrets.

Test:

1. Create post without image
2. Create post with image
3. Verify image uploads
4. Verify URL is stored
5. Refresh page
6. Verify image still appears

Fix all errors.

Then STOP.
